image: maven:3.9-eclipse-temurin-17

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/

stages:
  - test
  - build
  - deploy

test-job:
  stage: test
  script:
    - cd "Code Java/Trackr"
    - mvn test
    - awk -F"," '{ instructions += $4 + $5; covered += $5 } END { print covered, "/", instructions, " instructions covered"; print 100*covered/instructions, "% covered" }' target/site/jacoco/jacoco.csv
  coverage: '/(\d+\.?\d*) \% covered/'
  artifacts:
    paths:
      - "Code Java/Trackr/target/site/jacoco/"
    expire_in: 1 day

build-job:
  stage: build
  script:
    - cd "Code Java/Trackr"
    - mvn clean package -DskipTests
    - cp target/trackr-app-*.jar ../../trackr-app.jar
  artifacts:
    name: "trackr-app-$CI_COMMIT_REF_NAME"
    paths:
      - trackr-app.jar

pages:
  stage: deploy
  image: python:3.11-slim
  before_script:
    - apt-get update && apt-get install -y python3-pip
    - pip install mkdocs-material
  script:
    - mkdir -p Documentation_Site/Rendu-1 Documentation_Site/Rendu-2
    - cp CONTRIBUTING.md Documentation_Site/CONTRIBUTING.md
    - cp CHANGELOG.md Documentation_Site/CHANGELOG.md
    - cp "Documents et Rendus/Rendu 1/rendu1.md" Documentation_Site/Rendu-1/
    - cp "Documents et Rendus/Rendu 1/Personas1.jpeg" Documentation_Site/Rendu-1/
    - cp "Documents et Rendus/Rendu 1/Personas2.jpeg" Documentation_Site/Rendu-1/
    - cp "Documents et Rendus/Rendu 2/rendu2.md" Documentation_Site/Rendu-2/
    - cp "Documents et Rendus/Rendu 2/Screen UML.png" Documentation_Site/Rendu-2/
    - mkdocs build -d public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_BRANCH == "feature/maven-migration-trackr"
